#include <string>
#include <stack>

using namespace std;

class Node {

};

class Parser {
public:
    stack<Node*> toPostfix(string equation) {

    }
};

class Solver {
public:
    Solver(stack<Node*> postfix);

    float execute() {
        // TODO
    }
};